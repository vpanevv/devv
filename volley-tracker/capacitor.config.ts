import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vladimir.volleytracker',
  appName: 'VolleyTracker',
  webDir: 'www'
};

export default config;
